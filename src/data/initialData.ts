import { Product, Order } from "../types";

// Empty initial products array (removed sample data)
export const initialProducts: Product[] = [];

// Empty initial orders array (removed sample data)
export const initialOrders: Order[] = [];
